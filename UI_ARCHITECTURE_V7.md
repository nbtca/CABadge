# CABadge v7 UI 架构与版本范围

日期：2026-09-20。状态：架构方案第 7 版；用户已批准“PC LVGL 渲染 + USB 实板服务”及性能约束。核心实现已独立发布为 `7.0.0-preview`，实板联调待测；完整架构仍有后续验收项。

依据：用户已批准根目录 [DESIGN.md](DESIGN.md)，本轮要求先说明 v7 架构、新增、优化及与 v5 的区别。完整设计目标版本为 `7.0.0-ui`；当前核心预览源码版本为 `7.0.0-preview`。

## 1. 与并行 v6 的关系

- v5 对照依据是 [UI_DESIGN_V5.md](firmware/UI_DESIGN_V5.md) 和 v5 发布记录，不能用正在变化的工作区假冒 v5 原码。
- 本轮发现 [firmware/DESIGN.md](firmware/DESIGN.md) 为另一对话的 Gallery UI v6 方案；共用头文件已标记 `6.0.0-ui`。其方向是浅色背景、壁纸大入口加两个小入口、160 ms 位移动效。
- 根目录 `DESIGN.md` 的最新视觉修订是本轮 v7 依据。先前提出的“沿用 v5 暗底文字列表、仅优化尺寸”已被用户否决。v7 采用内容展示页、明亮操作面和连续转场；与 v6 可能共享品牌色，但不覆盖其工作，也不把 v6 成果直接改号冒充 v7。
- 后续实现须建立独立工作副本或独立工作树，并记录精确基线、共享接口及资源生成器版本。不得在正在开发 v6 的共用目录里直接改 `badge_ui.c`、字库、Web 页面或发布脚本。
- v6 的已验证服务修复可以在接口核对后合入 v7；不盲目复制整套视觉修改。v6 最终功能与稳定性目前未确认。
- v7 产物独立发布，保留 v5/v6。架构阶段不烧录设备、不调整现用工具快捷方式。

## 2. 总体架构

```text
主开发模式（用户选定）
  鼠标 / 键盘 → PC 上的共用 LVGL UI → SDL2 原生窗口
                         ↕ 动作请求 / 真实状态与事件
                        USB
                         ↕
  ESP32：已有 Wi-Fi / BLE / 电池 / 摇晃 / 壁纸 / 管理服务
                         ↕
              ESP-IDF 驱动、NVS、壁纸双槽

最终设备：同一份 LVGL UI → 板端本地服务 → 实体 LCD / 触摸
手机管理：现有本地 HTTP → 手机 Web → 同一组板端服务
其他验证：离线模拟数据；板端像素镜像（分别标识，不混同）
```

**本轮选择覆盖旧版“开发时必须 ESP32 渲染、PC 仅接收像素”的限定。** PC 编译并运行与固件共用的 LVGL UI、动画、字体及资源；ESP32 保持设备服务与持久化状态的权威。USB 不持续传屏幕像素或逐帧动画坐标，传动作、状态与必要资源。PC 画面明确是本机 LVGL 渲染，不能称作 ESP32 帧率。

NBTCA Badge TOOL 保留原生工具与烧录入口，可启动已有 SDL2 模拟器；首版无需把 SDL 窗口嵌入 Tk。一个时刻只有一个进程占用串口：联调期间由模拟器通信端持有，烧录前释放；旧像素镜像为单独测试模式。无需为此重建工具或改成 Web 模拟器。

手机 Web 使用适合浏览器的布局，与 LVGL 共用颜色语义、状态定义和设备服务，不共用像素布局或复制业务状态机。设备成功回执决定结果，页面不能自行判定连接或应用成功。

### 实板服务连接：复用项与缺口

本轮核对 `usb_telemetry.h/.c`、`usb_screen/protocol.h` 和设备 `receive()`：现有模拟器主要读取旧诊断文本，获得电压、Wi-Fi/BLE 连接状态和 RSSI，没有完整动作发送接口。现有二进制协议具有封包、长度限制、CRC、心跳，以及壁纸上传/确认和管理入口；无线扫描和连接主要通过板端 UI 的触摸/文本驱动。不能把这些当作已完成的 PC 实板联调。

| 能力 | v7 方案 |
|---|---|
| 共用 LVGL、SDL 显示、鼠标输入 | 复用；页面与动效在电脑实时运行 |
| 电压、连接状态、RSSI | 沿用字段语义，增加结构化状态快照；联调模式不靠抓串口日志驱动 |
| Wi-Fi 扫描、选网、连接、断开 | 增加语义命令和结果列表，调用现有板端服务；不模拟点击板端坐标 |
| BLE 广播/来访连接、扫描/主动连接 | 同上；明确区分两种角色、连接与广播状态 |
| 摇晃、息屏和唤醒 | 板端上报真实事件及相关状态；PC 息屏画面随之更新，只在息屏时摇晃唤醒，不恢复摇晃跳页 |
| 壁纸上传、保存、应用 | 复用现有格式、上传事务与回执；候选浏览在 PC，持久化成功由板端确认 |
| 已在板上的上传壁纸 | 初次连接/内容变化后按资源标识核对；缓存缺失才分块读取一次，需补读取接口。不得拿旧缓存或默认图冒充当前壁纸 |
| 手机管理入口 | 复用短时授权和既有服务，返回真实地址/授权状态，不绕过权限 |
| 预览亮度、真实背光 | PC 预览即时反馈；设置保存由板端确认，未实测背光能力仍不伪装为已完成 |

桥接只增加必要的服务调用适配。PC 与 ESP32 使用相同 UI 服务契约：PC 发送 USB 请求，最终固件调用本地服务。页面不含平台串口判断，不另建一套 Wi-Fi/BLE 业务逻辑，也不要求每个动画帧与板端页面状态同步。

通信沿用现有封包，握手增加模式、协议/能力版本与固件身份确认；新旧模式不能误把旧 READY 当成支持全部服务。请求用编号关联完成/失败，扫描和传图保留忙碌、取消、超时状态。参数与长度在板端校验，密码不写日志、不进入状态回传。断开后标记状态失效，重连先取完整快照，不自动重放上次连接、上传或开关动作。失去回执的持久化动作先查结果，不能直接重复执行。

USB 联调模式须关闭像素连续发送；动作和状态优先于分块资源同步，避免上传或读取壁纸堵住交互。未连接时明确显示“未连接设备”，不会自动填入看似真实的模拟数据。离线演示可保留为显式模式。

### 接近实机的受限预览（用户新增要求）

- 默认目标上限 **30 FPS**，以单调时钟控制约 33.33 ms 的刷新节奏；这是初始设计预算，不是已测得的 ESP32 帧率。当前 LVGL 默认刷新周期为 33 ms，本身不代表稳定 30 FPS。
- 圆屏渲染固定 **360×360、RGB565、LVGL 软件绘制**；放大窗口只放大已渲染像素，不增加逻辑分辨率。桌面工具区域不计入屏幕传输预算。现有模拟器 1040×760 ARGB8888 画布需要区分圆屏显示面与桌面控制，不宜直接拿整窗耗时代表实机。
- 固件参考配置为 ESP32-S3 240 MHz、8 MB Octal PSRAM、PSRAM 40 MHz；不通过 PC/MCU 主频比换算渲染速度。图像、中文字库、变换和圆形裁切与板端一致，不启用板端没有的 GPU 效果。
- 初始输出沿用 RGB565 的 16 行局部缓冲作为对比基线；最终 LCD 缓冲调整时同步修改预览。按实际提交的矩形像素累计带宽，不能看到圆屏就把全帧自动乘以 π/4。
- **40 MHz 四线 QSPI 仅作为待验证的目标链路预算**，不是当前板上已运行配置；当前点屏诊断为 10 MHz。40 MHz 一整帧纯像素约 12.96 ms，10 MHz 约 51.84 ms，均未含绘制和驱动开销。40 MHz 是否稳定必须接屏验证。
- 加入按像素量计算的传输等待预算，支持 10 MHz 诊断配置复现；耗时超限时自然降低更新频率，不在电脑上插帧或加速补帧。静止时不为凑满 30 FPS 强制刷新；动画按真实经过时间推进，降帧不把 240 ms 动画拖成长动画。
- 主线程继续处理输入与实板消息，不通过阻塞睡眠拖慢无线服务回执。限制 LVGL 圆屏更新与显示提交的节奏，不只是限制 SDL Present 次数；保持缓冲所有权直到模拟传输完成。
- 接屏前可以在实板运行同一场景、RGB565 软件渲染到内存，禁用像素镜像，测 Carousel、抽屉、名片等的绘制耗时，再校准电脑预览；此测试不需要屏幕，但仍不证明 LCD 时序或触摸已通过。
- 接屏后再测实际 flush、显示节奏和触摸输入延迟。软件刷新 FPS、屏幕扫描 Hz、端到端响应分开记录；仅限帧不是周期精确的 ESP32 仿真。

完整资料来源、屏幕扫描频率的条件估算、带宽算式和校准方法见 [动效研究第 5.1 节](MOTION_RESEARCH_V7.md#51-受限模拟器的刷新预算2026-09-20-补充)。本轮落实到架构约束，尚未修改模拟器代码。

## 3. 页面架构

### 圆屏

```text
壁纸展示 ← 横向拖动 → 蓝色名片展示
  默认首页                姓名 / 协会 / 标志
       ├─ 轻触：临时状态 + 可见的控制 / 功能入口
       ├─ 下拉：控制中心；上推收起并恢复刚才的展示页
       │    ├─ 状态：电池电压 / 无线摘要
       │    ├─ 快捷开关：Wi-Fi / 蓝牙
       │    ├─ 预览亮度滑条
       │    └─ 手机管理 / 立即息屏
       └─ 上滑：明亮操作面；下拖关闭并恢复刚才的展示页
            ├─ 壁纸：Carousel 环绕选择 / 预览 / 应用 / 手机传图
            ├─ 连接
            │    ├─ Wi-Fi → 网络列表 → 密码 / 连接结果
            │    ├─ 蓝牙 → 来访连接 / 主动连接 / 扫描列表
            │    └─ 手机管理 → 授权 / 同网或直连入口 / 二维码
            └─ 设置：显示 / 设备信息 / 立即息屏
```

名片恢复为独立展示内容，不藏进设备信息。首次启动仍显示壁纸；名片与壁纸切换无需打开管理页。首页横滑不再同时切换壁纸文件，多张壁纸在壁纸浏览页选择。操作面保留壁纸、连接、设置三种职责，视觉上由壁纸预览入口主导，另两项为较轻的操作；不再强制三条等大按钮。连接列表、密码输入和二维码为任务详情。临时状态默认 3 秒隐藏，触摸操作中不自动收走按钮。

### 壁纸 Carousel（用户指定）

壁纸选择页采用 [Bencho Carousel](https://bencho.dev/?c=carousel) 的核心观感：中央图片最大，两侧图片缩小、后退，拖动时沿二维弧线轮转，遮挡层级随位置改变，松手吸附。它不是仅淡入下一张或普通横向分页。去除持续漂浮和鼠标倾斜；保留图片比例和裁切规则。

先按已有两张内置壁纸加一张上传壁纸设计，不把双槽存储误当多图相册。浏览候选与应用分开，服务确认应用后回到纯全屏壁纸。名片保持独立展示页。小卡片露边不足以形成可靠热区时提供前后按钮；核心动作可通过明确点击完成。

二维轨迹、缩略图预算、七组组件选型及实际参考录像见 [v7 动效研究](MOTION_RESEARCH_V7.md)。该文给出的 Carousel 与组件专项参数补充下文通用转场参数。

### 下拉控制中心（本轮新增）

采用手机式抽屉交互：从展示页向下拖出，向上推回。它提供高频快捷操作；上滑操作面负责查找功能和进入详情。两者共用服务与设置，不各自保存一套开关状态。

| 区域 | 内容 | 交互 |
|---|---|---|
| 顶部状态 | 有效电池电压、简短无线状态 | 只读；没有有效数据时显示不可用，不推算百分比 |
| 无线快捷区 | Wi-Fi、蓝牙两个主要开关，配短状态文字 | 轻触开关；长按进入相应详情。上滑功能入口也能进入详情，长按不是唯一入口 |
| 亮度 | 一条横向预览亮度滑条，必要时显示数值 | 拖动实时更新既有画面亮度；实际背光未验证前仍称“预览亮度” |
| 下部操作 | 手机管理、立即息屏 | 前者进入现有授权/管理页；后者直接息屏 |

- 视觉：一个明亮面板，Wi-Fi/BLE 可采用两块计协蓝选中控件，其余操作较轻；靠尺寸和分组区别于旧版文字列表，不做九宫格或多层卡片。
- 完全展开后按圆屏完整页面布局；上下保留安全区与收起位置。若内容不足以满足 44 px 触摸下限，先缩短状态文案，不缩小触摸区或堆第二屏快捷图标。
- 蓝色表示功能开启，状态文字区分未连接、连接中、已连接；BLE 开启/广播不等于已连接。执行中等待服务结果，失败恢复真实状态。
- 长按初始目标为 450 ms；识别长按后不得再触发轻触开关。需以实机操作调校。
- 关闭正在承载手机管理或上传的无线连接时给出具体断开提示，按现有服务完成取消/清理；不能静默破坏进行中的上传。
- “手机管理”复用已有授权入口和短时窗口；不因快捷入口而默认常开热点或延长授权。
- 快捷面板不增加音乐、音量、手电筒等硬件不支持的控制。

### 手势与浮层关系

| 当前位置 | 手势 | 结果 |
|---|---|---|
| 壁纸 / 名片展示 | 横向滑动 | 在两种展示内容间切换 |
| 壁纸 / 名片展示 | 向下拖动 | 打开控制中心 |
| 壁纸 / 名片展示 | 向上拖动 | 打开功能操作面 |
| 控制中心 | 在收起区域向上拖动 | 关闭控制中心，恢复原展示页 |
| 功能操作面 | 在收起区域向下拖动 | 关闭操作面，恢复原展示页 |
| 无线列表 / 密码 / 详情页 | 控件自身手势与明确返回 | 优先滚动、输入或返回，不唤出全局抽屉 |

同一时刻只允许一个抽屉。控制中心/操作面打开时暂停横向切展示，临时状态层收起；面板不会因 3 秒计时自动关闭。滑条和开关先消费输入，不能同时拖动面板。松手未达到打开条件时回到原位，不把半开的面板当作一个新页面。

从控制中心进入 Wi-Fi、蓝牙或手机管理详情，返回控制中心；从功能操作面进入则返回原功能位置。使用一个明确的来源记录即可，不另建复杂路由系统。控制中心点击息屏后，下次唤醒直接恢复进入控制中心前的壁纸/名片，避免醒来又停在息屏按钮上；其他息屏来源保留原有恢复规则。

### 找回的蓝色名片

现有图像证据：[firmware/build/02-identity.png](firmware/build/02-identity.png)。它不同于后续源码备份中“圆形标志 + 姓名”的简化名片，不能将后者误认作用户指定版本。

- 保留原蓝色卡面的识别特征：左上 NBTCA / MEMBER、大号 Egger、计算机协会、右侧协会标志和弧形图案。
- 按圆屏安全区重排；姓名字号可沿用已存在的 36/56 px 字体，按实际字形和宽度选择，不新增大字库。
- 移除截图中仅用于演示的状态点/认证暗示，或明确标为示例；展示内容不代表通过会员认证。
- 本轮恢复展示页，不顺带添加在线成员系统、NFC、个人二维码或资料编辑后台。

### 视觉和动效验收目标

| Before：被否决的方向 | After：v7 修订 | Why |
|---|---|---|
| 暗底、等大的文字按钮组成整页 | 明亮操作面，壁纸入口为主，连接与设置降为次级；标题和正文形成清楚层级 | 页面构图有主次，改变旧版整体外观 |
| 删除旧名片 | 蓝色名片与壁纸并列展示，保留旧卡面标志性元素 | 恢复用户明确要求的展示能力 |
| 识别滑动后直接换页 | 拖动中内容跟手，松手后补全或取消 | 用户能看到页面的来源与去向 |
| 旧页隐藏后新页淡入 | 转场期间保留出入两页的位置连续性，完成后再隐藏非活动页 | 解决“硬切后补动画”而非只延长淡入 |
| 所有页面同一种进场效果 | 横向切展示、下拉控制中心、上滑功能面、详情沿进入方向返回 | 形成明确的空间关系 |

初始动效参数（目标，须经预览调校）：

| 动作 | 轨迹 / 时间 |
|---|---|
| 壁纸 Carousel | 拖动实时更新同一轮转进度，计算所有卡片的位置、缩放和前后层级；松手约 180–280 ms 吸附，可立即反向接续 |
| 壁纸与名片切换 | 拖动阶段与手指/鼠标位置同步；松手后依据距离和速度决定目标，约 180–240 ms 内补全或回位 |
| 操作面开关 | 由下进入、向下退出；拖动可取消；点击触发约 240 ms 无回弹缓出 |
| 控制中心开关 | 由上进入、向上退出；拖动跟手、可反向收回；松手后约 180–240 ms 补全或回位 |
| 详情进入/返回 | 进入时新内容从右进入，返回反向；约 200–240 ms，无空白帧 |
| 按下 | 立即改变局部底色；需要尺度反馈时仅作用于小控件，不缩放整屏 |
| 临时状态 | 短距离位移和透明度，约 160–180 ms；不遮盖主体姓名或照片中心 |

转场可打断，新操作从当前可见坐标接续，不用锁住输入来防冲突。默认无过冲、弹跳和实时模糊；减少动态时缩短或取消位移。LVGL 对象移动优先于整屏截图纹理，不为每页分配全帧缓存。USB 带宽下的效果单独记录，不宣称已经达到实体 LCD 帧率。

旧 USB 像素镜像受到带宽及阻塞影响。新选定的联调模式在 PC 运行共用 LVGL，USB 只交换服务数据与必要资源，因此不再用镜像带宽作为动效预览上限。分别记录 PC 动效、实板服务响应和最终板端渲染性能，不能以 PC 帧率代替实体 LCD 验证；见动效研究第 5 节。

### 手机 Web

```text
CABadge 本地管理
  ├─ 授权状态：待授权 / 有效 / 到期 / 断开
  ├─ 壁纸：选图 → 裁切与缩放 → 上传 → 保存 → 应用结果
  └─ 设备：基本信息 / 显示 / Wi-Fi / 蓝牙
```

最大宽度 460 CSS px，单列；只有预览为圆形。已授权后授权说明收起为一行。常规输入与主要按钮至少 48 CSS px 高，正文 16 CSS px、辅助文字 14 CSS px。文件选择和键盘继续使用系统能力，不增加框架或独立手机 App。

## 4. UI 内部职责

| 职责 | 负责 | 不负责 |
|---|---|---|
| 视觉规范 | 暗/浅语义色、字体、间距、控件状态 | 无线逻辑、持久化 |
| 页面与导航 | 展示页横向关系、上下抽屉互斥、连续转场、来源返回、手势归属、焦点 | 重新实现连接与上传状态机 |
| 临时交互状态 | 正在浏览的图片、输入内容、当前详情、提示计时 | 伪造设备状态 |
| 服务绑定 | 把已有快照显示出来，将动作交给已有回调 | 无线线程直接修改 LVGL |
| 资源与显示 | 现有字体生成、图像描述符、局部刷新 | 新图片协议、Flash 分区或硬件驱动 |

这是职责划分，不要求为每一项创建新文件或框架。优先在已有 `badge_ui.c/.h`、`wifi_panel.c` 和 Web 页面中使用少量共用样式与构建函数；生成字库仍经 `generate_assets.py`。

动效需要的结构调整限定为：出入页在转场期间同时存活；一次手势只有一个归属；每组联动对象共享一个进度；取消动画从当前可见值接续；对象与壁纸资源销毁前取消引用它们的动画。优先 LVGL 原生动画、滚动和吸附，仅在释放速度连续性确实不足时增加一个小型单变量收敛函数，不建立通用动画引擎。完整规则见 [动效研究第 4 节](MOTION_RESEARCH_V7.md#4-为动效调整的最小架构)。

## 5. 相对 v5 的增改

| 项目 | v5 已有 | v7 拟增加或优化 |
|---|---|---|
| 产品结构 | 纯壁纸、三入口菜单，名片被删除 | 恢复蓝色名片，与壁纸并列展示；三种管理职责放入上滑操作面 |
| 控制中心 | v5 无独立下拉快捷面板 | 增加下拉控制中心：无线开关、预览亮度、手机管理和息屏；与详情页共享真实状态 |
| 壁纸选择 | 常规浏览与应用 | 使用二维环绕 Carousel，中央大图、侧后方小图、跟手轮转与吸附；应用后仍为纯壁纸首页 |
| 品牌与构图 | 暗底、文字列表，层级较均匀 | 明亮操作面、内容优先的入口层级；蓝色名片成为独立视觉内容 |
| 圆屏布局 | 按页面布置坐标 | 全页面执行 R168 内容安全圆、R176 操作安全圆检查，检查完整矩形和真实字形 |
| 触摸 | 部分按钮只有 40/42 px 高 | 普通目标至少 44×44 px；主要列表行建议 56 px；检查扩展热区不重叠 |
| 状态层 | 轻触显示，3 秒收起 | 操作期间暂停收起，状态信息更少、更清晰 |
| 息屏 | 息屏摇晃唤醒，亮屏不跳转 | 保留；增加首触仅唤醒的防误触要求 |
| 中文 | 固定文案子集 | 缺字覆盖检查、明确占位和长名称详情；按资源预算评估 18 px 常用中文扩充 |
| 圆屏输入 | 紧凑键盘与电脑键盘 | 修正普通输入布局，保留手机/电脑输入；小键盘单列例外，不承诺此次完成新输入法 |
| 手机页面 | 选图、上传、状态控制已存在 | 统一字号与触摸尺寸、窄屏和字体放大适配、长名称换行、必要授权说明按需显示 |
| 操作反馈 | 已接入现有服务 | 明确显示连接中、传输、保存、应用、失败；100% 传输不能冒充已应用 |
| 管理入口 | 短时授权、二维码、直连路径 | 分步布局；检查二维码模块与静区；到期、断开、重新授权的页面状态清晰 |
| 动效 | 140 ms 淡入，用户反馈切换生硬 | 跟手拖动、方向一致的推入/退出、可取消与可打断；保留减少动态 |
| 性能验收 | 构建与既有回归 | 增加资源增量账本、运行时内存和几何/长文本检查，软件与实屏分别报告 |

核心变化是视觉构图重做、恢复蓝色名片、加入下拉控制中心以及连续交互；防误触、字库和反馈完善随之落实。Wi-Fi、BLE、传图、壁纸记忆、摇晃唤醒均为已有能力，不重复称为 v7 新功能。

## 6. 实现与验收顺序

1. 隔离 v7 工作区，核对并记录基线与服务接口；验证不写入 v6 目录。实现先打通 PC ↔ 板端握手、真实状态与 Wi-Fi 扫描/连接的最小闭环，检查断开、错误密码、超时和重连；未实现的服务明确不可用。
2. 架构经用户审核后，在该联调模式中做 Carousel、壁纸与蓝色名片横切、下拉控制中心、上滑操作面的共用 LVGL 实际预览和转场录制，验证视觉明显改变、跟手、反向接续、抽屉互斥和资源预算，再补一个详情页；不能仅凭静态截图进入全页面实现。补齐 BLE、摇晃、壁纸与管理服务的双向绑定，状态以实板为准。
3. 无线和管理详情；检查长网络名、双角色 BLE、密码输入、二维码与异常状态。
4. 手机 Web；检查 320/360/390/460 CSS px、200% 文字放大、键盘操作与上传状态。
5. 运行既有模拟器/协议回归、两端构建与资源对比，再产出独立 v7 固件与兼容工具。
6. 烧录后另做实板回归。真实 LCD、触摸、背光与无线负载通过须有对应反馈，不能沿用 v5/v6 或模拟器结果。

不新增 GIF、OTA、云账号、用户多图相册、BLE 传图、自动休眠或真实背光控制。既有接口提供什么能力，界面就如实展示什么能力；资源、授权与硬件边界继续遵守根目录 `DESIGN.md`。

## 7. 当前实现检查点（2026-09-20）

已在独立 [firmware-v7](firmware-v7/README.md) 实现核心 UI、受限 SDL/LVGL 预览、USB 服务桥和配套固件，8 项软件检查通过；[交接与待办](firmware-v7/HANDOFF.md)列出准确范围。原工作区 52 个基线文件哈希未变。未烧录或更新原快捷方式。

本次先供审核 [核心动效录像](outputs/cabadge-v7-preview-20260920/v7-motion.mp4)与实际预览。40 MHz/30 FPS 为待校准的候选预算，10 MHz 是保守档；PC 输出不能替代实屏性能。手机 Web 视觉、动态中文完整覆盖、QR 实扫、运行时内存峰值及真实 USB/无线/显示验收未完成，以上路线没有全部关闭。
