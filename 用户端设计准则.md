> 历史设计/阶段记录：保留当时版本、方案和测量值，不作为当前安装或验收说明。当前正式版为 v1.1.0 / 7.9.2-memory，参见[文档索引](docs/INDEX.md)。

# CABadge 电子吧唧 UI 设计与实现任务
你现在是一名具有成熟审美的消费电子产品 UI/UX 设计师，同时也是熟悉 ESP32-S3、嵌入式 GUI 和 LVGL 的固件开发工程师。
请基于当前项目已有代码，为 CABadge 电子吧唧重新设计并实现一套完整、简洁、美观、具有产品完成度的用户侧 UI。
## 一、项目定位
项目正式名称：**CABadge**
CA = Computer Association（计算机协会）。
CABadge 是为计算机协会设计的电子吧唧，以个性化静态与动态壁纸展示为核心，同时集成无线连接与基础设备管理功能。
它不是智能手表，不是电子宠物，也不是复杂的智能终端。
设计目标是打造一款具有成熟消费电子产品质感、能够体现计算机协会特色的个性化电子徽章。
本次任务不只是生成概念图，而是完成能够在实际固件中运行的界面。
请将工程中与该产品相关的旧项目名称统一为 CABadge，包括：
- 用户界面中的产品名称。
- 开机界面中的设备名称。
- BLE 广播名称（如需修改，注意兼容现有通信协议）。
- 设备信息页面中的产品名称。
- 相关用户文档。
不要盲目批量替换底层代码标识、依赖名称或第三方库中的字符串。
---
# 二、首先分析现有工程
修改代码之前，请先完整检查当前工程。
需要确认：
1. 当前开发框架、GUI 框架及具体版本。
2. ESP32-S3 的实际硬件配置。
3. LCD 驱动、分辨率、颜色格式及刷新机制。
4. 触摸驱动及现有交互方式。
5. Wi-Fi、BLE、USB 和电源管理模块。
6. 静态图片、GIF、动画解码及存储方式。
7. 现有 UI 架构、任务调度和内存管理。
8. 当前是否已经具备 USB 虚拟屏幕功能。
此前设计的目标屏幕为 360×360 圆形 LCD，请以工程中的真实配置为准。
目前 PCB 上的其他模块已经基本正常，但由于屏幕连接器购买错误，实体 LCD 暂时无法正常连接。
因此需要保留无实体屏幕环境下的开发和调试能力。
如果工程已经实现 USB 虚拟显示，应直接复用，不要随意重写。
**重要要求：不要为了重新设计 UI 而破坏现有正常工作的底层驱动和硬件功能。**
---
# 三、整体 UI 设计理念
设计关键词：
Minimal / Clean / Refined / Content First
目标是打造一套极简、精致、具有成熟消费电子产品质感的 UI。
整体应当简洁、克制、有美感，不追求浮夸的视觉表现。
可以参考优秀消费电子产品在排版、留白、信息层级和交互方面的设计理念，但不要直接照搬任何品牌的界面。
特别强调：
**不要做成常见的 AI 生成式概念 UI。**
明确禁止以下设计：
- 大面积渐变背景。
- 霓虹灯、赛博朋克和荧光描边。
- 玻璃拟态和过度透明效果。
- 大量悬浮圆角卡片。
- 复杂的九宫格功能图标。
- 密集的装饰线条。
- 无意义的图形、粒子和光效。
- 夸张的弹跳、缩放和转场动画。
- 为了填满屏幕而堆积元素。
不需要刻意突出所谓的科技感。
真正的美感应来自合理的布局、字体、间距、对齐和细节。
整体使用黑、白、中性灰作为基础颜色，允许一种低饱和度的强调色。
不能因为追求简洁就变得粗糙。
所有界面必须呈现统一的视觉语言。
---
# 四、产品信息架构
整个系统仅保留三个一级功能入口：
1. 壁纸
2. 连接
3. 设置
不要设计类似手机的桌面系统。
不要使用九宫格 App 启动器。
不要设置过多菜单层级。
产品的使用逻辑应当非常明确：
开机 → 显示壁纸 → 用户需要时打开菜单 → 完成操作 → 返回壁纸。
壁纸展示是产品的默认状态。
设置和连接功能不应占据用户的主要视觉注意力。
---
# 五、核心界面设计
## 1. 主界面：壁纸展示
这是整个产品最重要的界面。
开机完成后，默认进入壁纸展示界面。
要求：
- 全屏显示当前壁纸。
- 支持现有固件能够处理的静态图片和动态壁纸格式。
- 壁纸内容尽可能完整地展示。
- 默认隐藏所有 UI 元素。
- 不叠加常驻状态栏。
- 不强制显示时间、日期或天气。
- 不添加装饰边框。
- 不出现任何无意义的按钮。
对于动态壁纸，优先确保动画播放稳定。
图片显示应具有合理的缩放、居中和裁切策略，避免不必要的变形。
用户可以通过触摸手势切换壁纸，也可以进入壁纸管理页面进行选择。
设备状态应通过临时信息层展示，而不是永久覆盖在壁纸上。
不要添加电子宠物、情绪系统、虚拟角色养成或其他偏离产品定位的功能。
**主界面的最高原则：让壁纸本身成为视觉主体。**
## 2. 主菜单
在壁纸展示界面上滑进入主菜单。
主菜单只包含三个一级入口：
壁纸
连接
设置
采用简洁的文字列表布局。
要求：
- 不使用九宫格。
- 不使用大型彩色功能图标。
- 不采用多个圆角卡片堆叠。
- 不添加多余的页面装饰。
- 通过字体、间距、对齐和轻量分割体现信息层级。
所有可点击区域必须足够大，适合圆形触摸屏操作。
选中和点击时提供轻量反馈。
不要使用夸张动画。
应当能够快速返回壁纸展示界面。
## 3. 壁纸管理
这是用户进行个性化设置的主要入口。
提供以下功能：
- 查看当前壁纸。
- 浏览已安装壁纸。
- 预览壁纸。
- 切换壁纸。
- 设置默认壁纸。
壁纸缩略图应尽可能展示内容本身，不要在缩略图上覆盖大量文字或按钮。
壁纸列表需要根据实际素材数量选择合适的布局。
不要为了追求视觉效果而制作复杂的瀑布流、卡片墙或动画展示架。
复杂操作交给未来的手机端完成，例如：
- 上传图片。
- 上传 GIF 或其他受支持的动态素材。
- 删除和批量管理壁纸。
- 调整图片裁切与缩放。
- 管理本地素材存储。
现阶段必须以工程已经实现的功能为准。
尚未实现的功能可以预留接口，但不能做成看似可用、实际无效的按钮。
## 4. 连接中心
统一管理 Wi-Fi 和 BLE。
连接页面应清楚地展示设备当前的实际连接状态。
BLE 需要考虑两个独立功能：
A. Peripheral：允许手机主动连接 CABadge。
B. Central：CABadge 主动扫描并连接其他 BLE 设备。
如果底层尚未完整实现双角色能力，应结合当前蓝牙协议栈分析并逐步实现。
两种功能需要具有独立的控制逻辑。
不得将广播、扫描、连接中和已连接状态混为一谈。
界面只展示必要信息：
- Wi-Fi 状态。
- BLE 状态。
- 当前连接设备。
- 扫描和连接操作。
- 必要的断开连接操作。
不要在一个页面同时显示大量设备信息。
对于复杂的无线设置，优先考虑交由手机端完成。
所有界面状态必须与实际通信状态同步，不能使用伪造的连接状态。
## 5. 系统设置
系统设置只保留必要功能。
例如：
- 屏幕亮度。
- 自动休眠。
- 电池信息。
- 设备信息。
- 固件版本。
- 固件升级入口。
具体以当前硬件和固件实际支持的能力为准。
不需要添加复杂的开发者选项、性能监控仪表盘或花哨的系统页面。
设备信息页面中的产品名称统一使用：
CABadge
可以在适当位置标注：
Computer Association
但不要把完整英文名称强行放进所有页面。
品牌标识应该低调，不应占据壁纸展示空间。
---
# 六、圆形屏幕布局规范
目标为 360×360 圆形显示区域，以工程中的实际配置为准。
必须针对圆形屏幕进行真正的布局设计。
不能简单设计一个 360×360 方形界面，然后直接裁切成圆形。
具体要求：
1. 重要文字和按钮必须处于可见安全区域。
2. 页面顶部及底部需要合理避让圆形边界。
3. 主界面壁纸允许全屏显示。
4. 设置页面应控制文字行宽。
5. 列表项目必须具备足够的触摸面积。
6. 不允许出现文字或图标被圆形边缘截断的情况。
不要在屏幕内部再画一个圆形装饰框。
物理屏幕本身已经形成明确的圆形轮廓。
布局应考虑视觉重心，而不是机械地将所有元素绝对居中。
---
# 七、交互设计
整个系统的操作必须简单、一致。
建议交互：
主界面上滑：打开菜单。
主界面左右滑动：切换壁纸。
主界面轻触：临时显示设备状态。
菜单点击：进入对应页面。
下滑或明确的返回操作：返回上一级页面。
需要合理处理手势优先级。
壁纸切换手势不能影响设置页面中的滑动操作。
对于壁纸切换，应避免误触，并确保动画和资源切换过程不会造成内存泄漏。
不要将重要功能隐藏在用户难以发现的特殊手势中。
页面切换动画应当简短、轻量、流畅。
推荐简单的平移或淡入淡出效果。
不允许使用大幅度缩放、弹跳、粒子特效和不必要的动态装饰。
---
# 八、统一视觉规范
请建立一套统一且可维护的 UI 设计系统。
包括：
- 全局颜色变量。
- 字体及字号层级。
- 页面边距。
- 元素间距。
- 列表样式。
- 按钮样式。
- 状态反馈规范。
- 页面过渡参数。
不要在各个页面中随意硬编码不同的颜色、字体大小和间距。
尽可能复用基础 UI 组件。
特别关注中文显示效果。
确保中文字体清晰、字号适当、字重协调。
不要使用过小的辅助文字。
不要让标题、正文和说明文字缺少明显的信息层级。
不要在每一项设置之间都加入粗重的分割线。
**界面美感主要依靠排版和细节，不依靠装饰。**
---
# 九、固件性能与工程要求
UI 必须适配 ESP32-S3 的实际硬件能力。
要求：
 1. 优先使用当前工程已经采用的 GUI 框架。
 2. 不随意替换已经验证的 LCD 驱动。
 3. 根据当前 GUI 版本使用正确的 API。
 4. 尽可能使用局部刷新。
 5. 避免不必要的全屏重绘。
 6. 合理管理壁纸解码缓冲区。
 7. 避免频繁创建和销毁 GUI 对象。
 8. 无线通信不得阻塞 GUI。
 9. 壁纸切换不得造成内存泄漏。
10. 图片和动画加载失败时应具有合理的错误处理。
如果使用 LVGL，请先确认具体版本，禁止混用不同版本的接口。
目前实体屏幕无法连接，因此必须考虑现有的无屏幕调试方式。
如果工程已经有 USB 虚拟屏幕功能，应保证新 UI 能够正常在电脑端预览。
如果尚未实现 USB 虚拟屏幕，不要擅自将完整 USB 显示系统重构作为本次 UI 任务的前置条件，应先说明现状并优先使用可行的预览方式。
同时需要明确：
ESP32-S3 原生 USB Full-Speed 的带宽远低于常见配置下的板载 QSPI 接口。
USB 虚拟屏幕主要用于验证 UI、页面逻辑和渲染结果，不能用 USB 帧率评价真实 LCD 性能。
---
# 十、具体开发步骤
请按照以下顺序执行。
### 第一阶段：分析现有工程
阅读相关代码，确定 UI 架构、底层模块、资源管理及可复用组件。
明确哪些功能已实现，哪些仍需开发。
给出简洁的修改计划。
### 第二阶段：设计 UI
先确定页面结构、布局尺寸、字体层级、配色和交互逻辑。
如果当前环境支持界面预览，应生成实际可查看的预览，而不只是输出文字描述。
重点检查圆形裁切和中文排版。
### 第三阶段：实现 UI
在当前工程中完成：
- 壁纸主界面。
- 主菜单。
- 壁纸管理页面。
- 连接中心。
- 系统设置。
优先复用现有功能及组件。
不要另建一个无法集成到实际固件中的孤立 Demo。
### 第四阶段：功能整合
接入现有壁纸播放、触摸、Wi-Fi、BLE 和设备状态功能。
确保 UI 显示的内容来自真实数据。
对未实现的功能明确标注并预留接口，不制作虚假交互。
### 第五阶段：测试和优化
检查：
- 页面切换是否正确。
- 壁纸能否正常播放和切换。
- 圆形边界是否发生裁切。
- 中文是否显示正常。
- 触摸是否存在冲突。
- 动画是否阻塞系统。
- 内存占用是否合理。
- 已有通信功能是否受到影响。
完成项目支持环境下的编译和测试。
无法验证的硬件相关功能必须明确列出，不得宣称已通过实际测试。
---
# 十一、最终验收标准
最终 UI 应满足以下标准：
 1. 整体极简、美观、克制。
 2. 没有明显的 AI 模板化设计感。
 3. 默认界面完整展示壁纸。
 4. 不存在无意义的视觉装饰。
 5. 一级菜单只有壁纸、连接和设置。
 6. 页面层级简单。
 7. 所有文字和按钮适配圆形屏幕。
 8. 配色、字体和间距统一。
 9. 所有已实现功能均具有真实交互。
10. 不破坏现有硬件和通信功能。
11. 工程能够在现有环境下成功编译。
12. 可以通过实际可用的显示环境验证界面。
13. 项目对用户展示的产品名称统一为 CABadge。
完成后提供：
- 修改文件清单。
- UI 架构说明。
- 主要功能实现情况。
- 编译和测试结果。
- 尚未实现的功能。
- 需要等待实体屏幕恢复后验证的项目。
不要仅输出设计建议，请实际完成工程修改。
---
# 最重要的设计原则
CABadge 是计算机协会设计的个性化电子吧唧。
它的核心价值在于让用户展示自己喜欢的图片、动态壁纸和个性化内容。
**壁纸是主角，系统 UI 只是辅助。**
不要让菜单、状态栏、图标和装饰喧宾夺主。
不要追求看起来很复杂、很炫酷的界面。
请以成熟消费电子产品的标准完成设计与实现，重点追求克制、精确、统一、流畅和易用性。