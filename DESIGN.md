# CABadge DESIGN

> 状态：基础规范与 v7 架构已审核；核心 Preview 已实现，完整规范待验收 · 版本 0.7 · 2026-09-20  
> 适用：360×360 圆形 LCD 用户端、手机本地 Web 管理端。  
> 历史实现参照：`5.0.0-ui`；当前独立核心预览：`7.0.0-preview`；冻结 PCB：`c7c59dff`。实现范围与未完成项见 [v7 交接](firmware-v7/HANDOFF.md)，不代表全部规范已符合。

## v7 视觉修订：优先于下文旧版样式条款

用户明确否决“保持 v5 外观、仅修整细节”的方案，要求视觉明显重设计、连续页面过渡，并恢复早期蓝色名片。以下修订优先于本文原有暗色页面、纯文字主菜单、首页左右换壁纸和直接切换图片的建议；资源、真实状态、安全区、无障碍与硬件限制继续有效。

- 默认首页仍为纯壁纸。恢复独立的蓝色名片展示页，与壁纸横向切换；多张壁纸选择移到壁纸浏览页，避免同一手势承担两种用途。
- 壁纸选择页采用用户指定的 Carousel：中央大图、侧后方小图，二维环绕、缩放和前后遮挡随拖动连续变化，松手吸附。这是有任务目的的选图交互，不受下文禁止装饰卡片墙/直接切图的旧建议限制；应用后回纯壁纸。保留已有图片数量，不新增多图相册。
- 名片视觉依据是现存 [旧名片预览](firmware/build/02-identity.png)：蓝色卡面、大号姓名、协会文字、右侧标志与弧形图案。历史截图中的示例成员身份不等同于已认证身份，不新增真实认证或用户资料后台。
- 操作界面采用明亮中性底色，暂定画布 #F4F6F8、操作面 #FFFFFF、正文 #1B2430、次要文字 #5B6778、主要操作 #365B86。通过标题、分组与留白形成层级；不保留 v5 等粗、等大的黑底文字按钮列表。
- 工具入口为从底部进入的操作面，壁纸是主要入口，连接/设置为较轻的次级操作；保留三种管理职责，但不要求三个入口画成相同文字行。
- 新增下拉控制中心：从壁纸/名片展示向下拖出，上推收起；提供电池电压、Wi-Fi/BLE 快捷开关、预览亮度、手机管理及息屏。与上滑功能面分工，两个抽屉不能同时打开；滑条、列表及输入不触发全局导航。控制中心没有 3 秒自动关闭计时。
- 控制中心与详情页共用真实设备状态。进入详情后返回发起入口；控制中心执行息屏后唤醒回到原展示页。关闭承载管理/上传的无线连接须说明中断影响，不通过快捷入口绕过授权。
- 名片是内容本身，可保留其蓝色调、弧形图案与细微色阶；这是对品牌内容的明确例外，不扩展为系统界面装饰渐变或卡片墙。
- 横向展示页与纵向操作面拖动时跟手，松手后补全或退回；进入与退出方向一致。详情切换保留前后页面连续移动，不能先隐藏旧页再让新页淡入。
- 非拖动转场初始目标为 180–260 ms，采用无回弹缓出；中途反向操作从当前可见位置继续，不等待旧动画结束。减少动态继续可用。
- Carousel 释放吸附初始目标 180–280 ms；组件专项参数与 LVGL/USB 限制见 [v7 动效研究](MOTION_RESEARCH_V7.md)。小控件可有受控形变，仍不采用持续漂浮、实时模糊或真三维。动效按实际画面验证，USB 带宽不能由桌面补画掩盖。
- 用户确定 v7 主开发模式为 **PC 运行共用 LVGL UI + USB 连接真实板端服务**：电脑渲染、板端执行操作并保存状态；USB 传动作、状态与必要资源，不连续传像素。此选择替代旧版“电脑只接收板端像素”的开发约束；明确标识 PC 渲染，不冒称板端性能。保留像素镜像作为独立验证方式，最终固件编译同一份 UI。
- 用户要求预览尽量接近实机：圆屏固定 360×360 RGB565、软件绘制，默认 30 FPS 设计上限，结合实际更新像素量计算 QSPI 传输预算；40 MHz 是待验证目标，10 MHz 是旧诊断配置。板端绘制时间须测量校准，不能由 PC 主频比推算；降低帧率不改变动画总时长。完整依据见动效研究第 5.1 节，目前只制定约束，尚未实施限帧。
- 第一轮应交付共用 LVGL 的实际页面预览及转场录制，先验证外观和连续交互，再扩展所有设置页；静态截图不能证明转场流畅。

详细页面关系、动效与新旧差异以 [v7 架构修订](UI_ARCHITECTURE_V7.md) 为准。上述数值为初始设计参数，尚非实现或实测结论。

## 1. 产品与设计目标

CABadge 是计算机协会的个性化电子徽章。最重要的使用场景是展示用户选择的壁纸；连接和设置负责让展示可靠、操作方便。

设计优先级：**壁纸完整呈现 → 文字清晰与操作可靠 → 状态真实 → 视觉统一 → 动效。** 空间或性能不足时，先减少同时出现的信息和装饰，不能先缩小文字、触摸区域或省略失败状态。

- 产品名称统一为 **CABadge**；Computer Association 只在设备信息中作为辅助说明。
- 电脑原生工具名称保留 **NBTCA Badge TOOL**。本规范不把它或原生 LVGL/SDL2 模拟器改成网页。
- 系统界面采用中性色与计协蓝；用户壁纸保留原色，不强加品牌滤镜。
- 不做手表表盘、应用桌面、九宫格、仪表盘或电子宠物。
- 不使用系统装饰渐变、霓虹描边、玻璃拟态、卡片墙、粒子、光晕和弹跳。壁纸自身包含这些内容不受限制。
- 本文与旧任务书冲突时，以用户本次“仅产出设计文档、等待审核”的要求为准。未实现能力不能通过一个可点击的假入口来“预留”。

## 2. 参考研究与取舍

参考文件来自 VoltAgent 的社区网站分析，标记为 alpha；**不是 Apple、Vercel 或 Linear 发布的官方设计手册**。其中包含营销站布局和自动提取的组件示例，不能直接当作圆形嵌入式设备规范。

| 参考 | 提取的原则 | 转化为 CABadge 的规则 | 不采用的部分 |
|---|---|---|---|
| [Apple 分析](https://github.com/VoltAgent/awesome-design-md/blob/main/design-md/apple/DESIGN.md) | 图像优先；操作元素退后；用排版组织层级 | 首页默认只有壁纸；操作按需出现；标题、正文、辅助信息各有固定字号 | 营销大标题、大段留白、SF Pro 字体资源、品牌蓝、负字距及按钮缩放照搬 |
| [Vercel 分析](https://github.com/VoltAgent/awesome-design-md/blob/main/design-md/vercel/DESIGN.md) | 中性色分层；组件尺寸一致；技术信息有独立表达 | 复用少量语义色与控件；调试细节留在工具中；管理页按任务顺序排列 | 网格渐变、堆叠阴影、营销胶囊、多栏展示、整套庞大颜色阶梯 |
| [Linear 分析](https://github.com/VoltAgent/awesome-design-md/blob/main/design-md/linear.app/DESIGN.md) | 暗底层次克制；强调色集中用于行动；内容对齐精确 | 圆屏使用两级中性底色；列表靠间距和文字组织；选中、进行中与失败有明确区别 | 紫色品牌、极密桌面界面、超大标题、强制暗色手机网页 |

CABadge 的辨识度来自圆形内容画面、计协蓝与简洁文字导航。无需让用户认出任何参考品牌。

## 3. 已核对的工程基线

以下是当前源码、配置和记录的状态，不是设计推测。

| 项目 | 当前依据与限制 |
|---|---|
| 主控与存储 | ESP32-S3R8；16 MB Flash、8 MB Octal PSRAM。总容量不能等同于 UI 可用预算 |
| GUI / SDK | LVGL **9.4.0**、ESP-IDF **5.5.0**；Windows 模拟器使用 SDL2 **2.32.10** 与共用 UI 源码 |
| 显示路径 | 360×360；板端渲染 RGB565 变化区域，通过 USB Serial/JTAG 传到电脑，电脑回传输入 |
| 实体屏幕 | LCD、触摸及背光等待连接器问题解决后实测；当前 USB 预览不能证明实体屏幕效果或 QSPI 帧率 |
| 图片能力 | 两张内置静态图片与一张上传图片；上传内容固定为 360×360 RGB565 |
| 持久化 | 壁纸分区 1 MiB，双槽保存同一张上传壁纸；不是两个用户相册位置。已实现壁纸选择记忆 |
| 无线管理 | Wi-Fi 本地 HTTP 传图与管理；BLE 轻量状态与控制；保留一条来访、一条主动 BLE 连接的现有边界 |
| 授权 | 设备端明确开启的短时管理窗口，当前为 5 分钟随机授权码；手机不能自行获得控制权限 |
| 显示设置 | 当前“预览亮度”影响画面，不能称作真实背光调节；UI 息屏不能称作 MCU 深度睡眠 |
| 暂未实现 | GIF/视频、用户多图相册、OTA、云账号、自动休眠与完整低功耗后端 |
| 实测口径 | 既有 v4 用户反馈与当前 v5 软件检查分别保留；v5 实板回归和实体显示体验待测 |

硬件、电源与充电控制不在本设计范围内；不能为实现视觉效果变更冻结引脚、LCD 初始化或现有充电策略。

## 4. 共用视觉语言

### 4.1 颜色

品牌依据为本地 [logo.png](firmware/ui/logo.png) 不透明像素众数 **#124689**。这是现有标识取色，不声称是协会官方品牌手册的精确标准值。使用同一蓝色方向的明暗变体，适配不同底色。

| 语义 token | 圆屏暗色 | 手机 Web 浅色 | 用途 |
|---|---|---|---|
| `brand.source` | #124689 | #124689 | 标识依据，不直接铺满界面 |
| `canvas` | #101112 | #F5F6F8 | 设置与管理页面背景 |
| `surface` | #202224 | #FFFFFF | 输入或必要的临时操作面 |
| `text.primary` | #F2F3F4 | #20252D | 标题、正文、主要数值 |
| `text.secondary` | #A6AAAF | #59677A | 辅助说明、单位、次要状态 |
| `accent` | #8CAED8 | #365B86 | 主要操作、选中标记、焦点 |
| `on.accent` | #101112 | #FFFFFF | 实心强调按钮上的文字 |
| `selection` | #263749 | #E5EAF0 | 选中行或次要按钮底色 |
| `divider` | #303337 | #D6DFE9 | 非必要识别边界、轻分割 |
| `border.control` | #A6AAAF | #59677A | 输入框等需要明确识别的边界 |
| `text.error` | #D88E8E | #A63D40 | 失败与危险操作文字，仅小面积使用 |

颜色规则：

1. 蓝色是唯一品牌强调色；错误红仅承载语义，不增加绿色成功卡片、橙色提示条等第二套装饰色。成功用文字或勾选表示。
2. 不能只用颜色区分状态：选中要有“使用中”或选中标记，连接成功要有“已连接”。
3. 默认列表透明，按下或选中才使用局部底色；不为每一行创建独立悬浮卡片。
4. 弱分割线不能作为输入框、焦点或可点击区域的唯一识别依据。键盘焦点用强调色 2 px 外框，手机偏移 3 px。
5. 文字不得直接覆盖不可预测的壁纸区域。临时状态使用不透明中性底，保证对比度；不依赖实时毛玻璃。
6. 禁用控件保留可读标签并就近解释必要原因，不把整组控件透明度降到难以辨认。

按 sRGB 相对亮度公式计算的基础配色检查：

| 前景 / 背景 | 对比度 |
|---|---:|
| #F2F3F4 / #101112 | 17.01:1 |
| #A6AAAF / #202224 | 6.83:1 |
| #8CAED8 / #202224 | 6.96:1 |
| #101112 / #8CAED8 | 8.25:1 |
| #20252D / #F5F6F8 | 14.24:1 |
| #59677A / #F5F6F8 | 5.33:1 |
| #FFFFFF / #365B86 | 7.01:1 |

设计验收目标：常规文字至少 4.5:1，必要控件边界和状态图形至少 3:1。上述结果只验证色值配对；不代替 RGB565、实际背光、视角、阳光下的可读性测试。

### 4.2 字体与字号

**圆屏复用 Noto Sans SC 子集。** 不嵌入 SF Pro、Linear 字体或额外西文字库；不通过更换字体掩盖布局问题。中文默认字距 0，不照搬英文营销标题的负字距。

| 圆屏角色 | 字号 / 字重 | 当前生成字体行高 | 使用规则 |
|---|---|---:|---|
| 辅助信息 | 14 px / 500 | 19 px | 单位、版本、次要状态；不用于主要操作 |
| 正文与按钮 | 18 px / 500 | 23 px | 列表、按钮、输入、连接状态 |
| 页面标题 | 24 px / 600 | 32 px | 每页一个主标题；不要同时堆叠多个大标题 |
| 产品名称 | 36 px / 600 | 46 px | 仅设备信息等低频页面 |
| 已有 56 px 字体 | 56 px / 700 | 70 px | 当前资源存在，本规范不新增使用场景 |

布局使用实际 `lv_font_t.line_height`，不能将 24 px 字号当成 24 px 高的中文文字框。多行 18 px 正文建议额外行间距 3 px；按钮按真实字形视觉居中。重生成字库后重新检查行高。

**手机 Web 使用本机系统字体**，建议字体栈：`system-ui, -apple-system, "Segoe UI", "PingFang SC", "Microsoft YaHei", sans-serif`。不下载网络字体，保持离线可用。

| Web 角色 | 字号 | 字重 | 行高 |
|---|---|---|---|
| 页面标题 | 1.75rem（默认 28 CSS px） | 600 | 1.25 |
| 分组标题 | 1.25rem（20 CSS px） | 600 | 1.4 |
| 正文 / 按钮 / 输入 | 1rem（16 CSS px） | 400 / 600 | 1.5 |
| 辅助信息 | 0.875rem（14 CSS px） | 400 | 1.5 |

Web 保留浏览器缩放和用户字体设置；200% 放大时内容可换行、按钮可增高。只有地址、授权码等需要逐字核对的信息允许使用系统等宽字体。

长内容处理：SSID、设备名在圆屏列表中单行省略，完整名称放到详情或手机端；不缩小字号，也不自动跑马灯。截断必须遵守 UTF-8 字符边界；显示处理不能改动连接所使用的原始名称或密码。

### 4.3 间距、形状与图标

- 间距基准：**4 / 8 / 12 / 16 / 24 / 32 px**。4 用于紧密关联信息，8 用于控件间隙，12–16 用于内部留白，24–32 用于分组。
- 圆屏一般控件圆角 12 px，输入框 8 px，临时面板 16 px；列表容器不额外加圆角外框。
- Web 按钮圆角 12 CSS px、输入框 8 CSS px；内容分组靠标题和留白，不靠层层卡片。
- 圆形仅用于壁纸裁切、必要的圆形图标按钮和开关滑块；不把每个操作都做成胶囊。
- 图标使用单色、约 2 px 线宽；圆屏 20–24 px，Web 20 px。采用已有且覆盖的符号/图形，禁止以彩色 emoji 代替一致的图标。
- 文本标签优先于图标猜谜。图标按钮必须具备可访问名称；实心与描线图标不要无规则混用。

## 5. 360×360 圆形 LCD 布局

### 5.1 几何安全区

本节所有 px 均为 **360×360 画布上的像素**，不是 CSS px、iOS pt 或毫米。以圆心 `(180,180)`、物理半径 `180` 为设计模型。

- 壁纸可铺满物理圆；系统文字与关键图标置于半径 **168 px** 的内容安全圆内。
- 独立控件的完整点击矩形置于半径 **176 px** 的操作安全圆内，并保证相邻命中区域不重叠。
- 检查矩形四角：`(x-180)² + (y-180)² ≤ R²`。不能只检查控件中心，也不能把整个屏幕统一内缩 12 px 就视为安全。
- 某高度 y 的安全半宽为 `sqrt(R²-(y-180)²)`；越靠近上下端，布局越窄。
- 安全区是初始工程余量。真实面板遮挡、装配公差和触摸边缘性能未验证前，不能称为最终物理可用区。

以下是**待实现的标准布局样例**，不是当前源码坐标清单。坐标格式为 `(x,y,w,h)`：

| 元素 | 样例矩形 | 说明 |
|---|---|---|
| 返回按钮 | `(88,40,44,44)` | 使用返回符号；其命中矩形在安全区内 |
| 页面标题 | `(140,44,132,32)` | 24 px 字体；长标题改短称，不挤压返回按钮 |
| 三项主菜单 | `(64,96,232,56)`、`(64,160,232,56)`、`(64,224,232,56)` | 行间 8 px，内部水平留白 12 px |
| 页面底部主要操作 | `(112,296,136,44)` | 外部命中框按 R176 检查；文字另按 R168 检查 |
| 壁纸预览 | `(116,92,128,128)` | 圆形缩略图；名称和动作放图外 |

列表滚动到圆边的内容可被裁切，但未完整进入安全区的行不得承担关键可点击操作；焦点移动时将整行滚入可操作区。无线列表与普通菜单可有不同纵向范围，不能强行共用一个方形布局。

### 5.2 页面结构与默认行为

```text
壁纸展示（默认）
  ├─ 轻触 → 临时状态 + 菜单入口
  └─ 上滑 → 主菜单
               ├─ 壁纸 → 浏览 / 应用 / 手机传图
               ├─ 连接 → Wi-Fi / 蓝牙 / 手机管理
               └─ 设置 → 显示 / 设备信息 / 立即息屏
```

| 页面 | 布局与内容规则 |
|---|---|
| 壁纸展示 | 无常驻标题、电池、网络、时间、边框和按钮。等比裁切、不拉伸；开机恢复有效选择，素材异常回退内置壁纸 |
| 临时状态 | 使用一个紧凑中性面板，最多两行状态和一个菜单按钮；默认 3 秒后隐藏。正在触摸时不突然收走按钮，松手后重新计时 |
| 主菜单 | 只有壁纸、连接、设置三条文字行。无第二组快捷入口或大型彩色图标 |
| 壁纸 | 一张预览、简短名称、前后浏览与应用。浏览不立即替换首页；应用成功后回首页；当前项显示“使用中” |
| 连接 | 一级页只给无线摘要和入口；Wi-Fi、BLE、手机管理各自进入详情，不将所有设备列表挤在一屏 |
| 设置 | 仅展示已实现能力；目前使用“预览亮度”“减少动态”“设备信息”“立即息屏”。不出现不可用的 OTA 或自动休眠开关 |
| 手机管理 | 分步展示授权、地址或二维码、必要的热点信息与关闭入口。二维码与完整凭据不在狭窄区域互相争空间 |

二维码使用黑白高对比、整数像素模块和足够静区，静区至少 4 个模块。当前 136 px 展示区域需要按实际二维码版本核算；放不下时单独进入二维码页，不能任意压缩静区。长授权码分行完整显示，不能省略；关闭管理或到期后不再展示有效样式。

### 5.3 触摸与输入

- 独立按钮命中区域至少 **44×44 px**，主要列表行建议高 **56 px**；相邻独立目标间距至少 8 px。
- 可见开关可为 48×28 px，但其操作区域至少 48×44 px；滑条轨道可为 4–6 px，整条命中高度至少 44 px。
- 命中区域扩展仍须检查圆边、层级和重叠。不能靠不可见的重叠热区宣称满足尺寸要求。
- 44 px 是本项目初始像素标准，不保证真实手指操作体验；需按屏幕有效直径换算毫米并上板验证。
- 当前紧凑全键盘无法同时容纳十列字符和上述触摸下限，明确记作**现有例外/待整改项**。不得宣称它已达标。
- 常用路径优先选择网络后输入密码；保留 USB 电脑键盘，已打开的手机管理端使用手机系统键盘。首次配网可走设备明确开启的临时热点管理路径，不依赖设备已联网这一前提。
- 若要求脱离手机/电脑独立输入，需单独审核分页字符布局或其他大目标键盘；本稿不擅自增加一套输入法。

## 6. 手机 Web 管理端布局

手机页是操作工具，使用矩形浏览器画布；**只有壁纸预览是圆形，不把整页套进圆屏模型**。

### 6.1 容器与信息层级

- 单列，页面外框最大宽 **460 CSS px**，居中；沿用当前体量，不新增桌面侧栏或多列仪表盘。
- 视口宽 320–359 px 时左右留白 16 px；≥360 px 时 24 px。顶部留白 24 px，底部至少 `24px + env(safe-area-inset-bottom)`。
- 超出 460 px 的空间留在页面外；窄屏、横屏与字体放大允许纵向滚动，不固定整页高度。
- 顶部显示 CABadge 和简短“本地管理”，下方只有“壁纸 / 设备”两个标签。标签高至少 48 px，以文字与下划线表示选中。
- 一级标题距导航 24 px，分组间 32 px，标签与控件间 8 px，相关按钮间 8–12 px。
- 授权未完成时，授权区清晰展开；已授权后压缩为一行状态。不要让长期说明文字占据壁纸操作区。

### 6.2 两个页面

**壁纸页顺序：** 授权状态 → 标题 → 圆形裁切预览 → 选择照片/居中 → 缩放 → 上传并应用 → 操作状态 → 恢复默认壁纸。

- 圆形预览宽度 `min(100%, 360px)`，保持 1:1；输出始终为 360×360，不随手机像素密度改变协议尺寸。
- 拖动和缩放均有替代控件：缩放条、居中按钮、键盘方向键。手势只在预览内消费，不锁住整个页面滚动。
- 文件选择、输入框、滑条优先使用浏览器原生控件。普通触摸目标至少 44×44 CSS px，主要按钮与文本输入高至少 48 CSS px。
- 同一时刻只有一个强调色主操作。没有照片或未授权时不可上传，并在相应位置说明原因。
- 选择图片后不能将本地裁切预览标成“设备当前壁纸”；收到应用结果后才表述设备已更新。

**设备页顺序：** 名称/固件/有效电池电压 → 显示 → Wi-Fi → 蓝牙。

- 使用标题、键值行和普通列表；禁止每个数值一张卡片。
- 长 SSID、BLE 名称和地址可换行；名称与操作按钮分行以保留完整触摸目标。
- Wi-Fi 选中网络后展开对应连接表单，密码默认隐藏，提供明确的显示/隐藏按钮；取消保留网络列表位置。
- BLE 区分“连接到本机”和“本机连接的设备”，并独立展示扫描进度；不能用一个“已连接”掩盖两个角色。
- 关闭当前承载 Web 的 Wi-Fi 或管理入口前，给出一次具体确认：“关闭后，手机将与设备断开。”不要对每次切换都弹确认框。
- 状态请求失败时标注“连接中断 / 状态未更新”，不能把最后一次成功值继续当作实时数据。

## 7. 组件和交互契约

### 7.1 共用状态

| 状态 | 视觉 / 文案 | 行为 |
|---|---|---|
| 默认 | 中性正文，主要操作使用蓝色 | 可点击区域和含义明确 |
| 按下 | 局部底色变化或细边变化 | 按下即反馈；松手确认，不缩放整页 |
| 选中 | `selection` 底 + 标记或“使用中” | 不与“正在连接”共用表现 |
| 进行中 | 动作名称，如“正在连接…”“正在保存…” | 禁止重复提交；只有后端可取消时才给取消入口 |
| 完成 | 简短结果，如“已应用” | 以设备成功回执为准；普通成功信息约 3 秒后收起 |
| 失败 | 错误文字 + 可执行操作，如“连接失败，重试” | 失败原因留到用户处理；原壁纸与有效设置保留 |
| 不可用 | 可读的次要样式 + 必要原因 | 不响应；不能仅依赖 hover 提示 |

上传进度只表示已传输字节；到 100% 后可能仍需校验、保存和应用。仅收到最终应用确认才显示“已应用”。失败或取消不能先清空旧壁纸；提交无法撤回时不要显示仍可取消。

错误文案面向任务，不在产品页展示堆栈、GPIO、CRC 数字、COM 端口调试提示或持续日志。“授权已过期，请在设备上重新开启”比一整段协议解释更有用。诊断细节仍可存在于电脑工具的相关区域。

### 7.2 手势与导航

- 首页上滑进菜单、左右滑换壁纸、轻触显示临时状态；临时状态提供可见菜单入口，避免唯一入口依赖手势记忆。
- 所有子页有明确返回；下滑返回只在页面未处理纵向滚动且手势起点属于导航区域时生效。
- 输入框、滑条、开关、列表和裁切区域先处理自身手势；不能冒泡触发换页。
- 横向位移达到 24 px 且大于纵向位移的 1.3 倍，可作为待校准的首页换图初始阈值；单次手势最多切一张。现有识别器先对照验证，不因文档强制重写。
- 息屏摇晃恢复原页面和原壁纸；亮屏摇晃不导航、不切图。首个唤醒触摸不同时触发底下按钮，是待实现的防误触要求。
- 扫描、连接和 Flash 写入不阻塞触摸反馈。离开页面后晚到的结果不能把用户强行送回连接页。

### 7.3 动效与无障碍

| 动作 | 圆屏 | 手机 Web |
|---|---|---|
| 按下反馈 | 下一次 UI 刷新即反馈 | 即时状态变化；可用 100–120 ms 颜色过渡 |
| 页面切换 | 140 ms 轻淡入；无弹跳、无全屏缩放 | 标签切换直接完成，不增加大型转场 |
| 壁纸切换 | 优先直接切换；避免为交叉淡化保留两套大图 | 裁切预览即时更新 |
| 减少动态 | 取消过渡，保持状态变化 | 响应 `prefers-reduced-motion`，保留可见焦点 |

动画可被新操作打断，不能排队积累。没有可测的完成进度就显示阶段文字，不能循环伪造百分比。圆屏不以 60 FPS 为未经实测的交付承诺。

Web 使用语义化按钮、标签、状态区和顺序合理的键盘焦点；无 hover 才可发现的必要操作。错误与连接状态可通过 `aria-live` 通知，周期状态轮询不能反复抢焦点或播报全部数值。

## 8. 嵌入式资源与实现边界

### 8.1 字库策略

当前 [generate_assets.py](firmware/generate_assets.py) 从指定源码收集字符并生成 14/18/24/36/56 px 字体，使用 8 bpp 字形数据；[assets.c](firmware/ui/assets.c) 是生成产物，不能手动修改。

- 固定 UI 文案加入源文件后通过原生成器更新；标点、单位、错误状态也必须纳入检查。
- 当前子集不能保证任意中文 SSID/设备名完整显示，更不涵盖全部 Unicode 与 emoji。
- 后续扩展先统计实际缺字；常用中文优先只扩充 18 px 正文字号，按构建增量审核。不能同时打包五套完整 CJK 字库。
- 未覆盖字符采用明确的缺字占位，并允许在手机端查看完整名称；无法从名称辨认时辅以稳定的地址/标识。不要静默删字导致两个设备看起来相同。
- 字体大小优化或 bpp 改动必须经过真实中文截图与 Flash 增量比较，不能只凭体积决定。生成新字库后重新核算行高与裁切。

### 8.2 资源账本

以下二进制单位按 `1 KiB = 1024 B`、`1 MiB = 1,048,576 B` 计算。

| 资源 | 已知大小 / 当前约束 |
|---|---|
| 一张 RGB565 壁纸 | `360×360×2 = 259,200 B`，约 253.125 KiB |
| 两张 RGB565 全帧缓存 | 518,400 B，约 506.25 KiB；不能当作零成本转场 |
| 一张 ARGB8888 内置素材 | `360×360×4 = 518,400 B`，约 506.25 KiB 原始像素 |
| 当前两张内置全尺寸素材 | 共 1,036,800 B 原始像素，主要计入固件资源；不是必然存在两份 PSRAM 解码副本 |
| 当前 USB 局部绘制缓冲 | `360×16×2 = 11,520 B`；使用现有局部刷新，不改成全屏双缓冲 |
| 应用分区 | 从 `0x10000` 起，大小 `0x700000`，即 7 MiB |
| 壁纸分区 | 从 `0x710000` 起，大小 `0x100000`，即 1 MiB；保留现有双槽原子提交格式 |
| v5 构建报告 | Flash 3,123,909 / 7,340,032 B；静态 RAM 89,648 / 327,680 B，见现有构建日志 |

构建报告不是运行时峰值。8 MB PSRAM 包含其他用途，仍需测量剩余空间与最大连续块；不能据上表承诺还可放多少图片。

本草案新增的资源验收预算：

1. 纯样式和布局改动原则上不增加全尺寸位图缓存；缩略图优先复用已有图像描述符与缩放。
2. 本轮设计落地若因字体/资源使应用相对基线增长超过 **512 KiB**，先提交资源差异供审核；这是评审阈值，不是硬件极限。
3. 应用分区建议至少保留 **20%** 空间作为维护余量；不得为了新图标或字库擅自改分区。
4. Wi-Fi、BLE、上传和 UI 同时运行时，记录内部堆与 PSRAM 最低可用量、最大可分配块、失败次数；重复切页/换图后空闲内存不能持续下降。运行时余量阈值需用实测峰值确定，不在本稿虚构通过数值。
5. 复用现有 UI 线程、服务状态与事件传递。无线回调不直接操作 LVGL；Flash 写入和图片接收不占用 UI 的阻塞循环。
6. 实体 LCD 接入后重新核对 DMA 与显示缓冲的内存能力要求。当前 USB 路径的 PSRAM 分配不能直接作为 QSPI DMA 正确性的证明。

手机完成 JPG/PNG 解码、裁切和 RGB565 转换，设备接收固定像素数据。沿用当前 20 MiB 源文件与 40,000,000 像素限制；不引入板端照片解码、在线字体、CDN 或新 Web 框架。

### 8.3 功能与安全不能被视觉简化

- BLE 的“已连接”“已授权控制”是不同状态；不把短时码授权称为持久配对或加密连接。
- Wi-Fi 扫描、关联、获得地址与本地管理可用分别展示真实结果，不把扫描到网络当作连接成功。
- 授权只在设备端明确开启。关闭、到期或重启后需按现有协议重新授权；不能为了少一步操作设置永久通行码。
- 二维码可帮助打开入口，但不能绕过授权。凭据不出现在普通日志或错误截图文案中。
- 当前 HTTP 为本地管理用途，不宣传为公网远程管理或端到端加密服务。
- 电池信息优先显示有效实测电压，如 `4.03 V`；没有可靠估算模型时不伪造电量百分比或剩余小时数。

## 9. 与现有 v5 的差异及落地次序

**本节是后续整改清单，不是本次已完成内容。**

| 项目 | 当前情况 | 审核后应做 |
|---|---|---|
| 内容与品牌 | 已有壁纸优先、三入口、计协蓝 | 保留，按本规范统一边缘细节 |
| 圆屏布局 | 既有菜单 x52/w256/h62，与本稿样例不同 | 对每页文字和命中框做圆形几何检查；不只替换主菜单 |
| 临时状态 | 当前约 3 秒隐藏 | 核对按住按钮时是否暂停隐藏；不足则修正 |
| 触摸面积 | 一些 40/42 px 高按钮、紧凑键盘存在例外 | 普通按钮达标；键盘单独决策，先保留可靠替代输入 |
| 中文名称 | 仅静态文案子集有保障 | 增加缺字检查与明确回退，评估单字号常用中文 |
| 手机排版 | 已有 460 px 单列、两标签；若干文字为 12–15 px | 按 16/14 px 层级、48 px 主控件和窄屏规则调整 |
| 颜色状态 | 现有代码有各处分散色值 | 收敛到语义 token；检查禁用、错误和控件边界对比度 |
| QR 与凭据 | 现有 QR 区域约 136 px | 按版本/模块数/静区验证并用手机扫码；必要时独立页面 |
| 性能与实物 | 已有软件构建与预览，实体显示待测 | 增补实屏和并发内存数据，不替换原实测结论 |

审核后建议顺序：先统一 token 与中文排版 → 圆形安全区和触摸目标 → 手机传图/设备页 → 状态、异常、无障碍回归 → 实体屏幕验证。继续复用现有实现，不为应用这份文档新增组件框架或第二套 UI。

## 10. 验收清单

以下勾选项均为待执行标准；文档发布时不预勾选。

- [ ] 默认首页只有壁纸；冷启动恢复有效选择，错误可回退。
- [ ] 轻触状态自动收起且不打断点击；菜单仅壁纸/连接/设置。
- [ ] 明确返回、手势优先级、息屏摇晃、唤醒防误触符合约定。
- [ ] 所有静态文字与独立命中矩形通过安全圆检查；实际圆形截图无关键内容裁切。
- [ ] 普通触摸目标达标；键盘例外已被明确处理，不能以“鼠标可点”代替手指验收。
- [ ] 中文、长名称、空名称、缺字和错误文案分别检查；文字不通过缩小来强行适配。
- [ ] Web 在 320 / 360 / 390 / 460 CSS px 宽度、横屏、200% 文字放大下无关键控件溢出。
- [ ] Web 键盘可完成选择、裁切调整、输入与提交；焦点可见，状态播报不过量。
- [ ] 上传的接收、保存、应用有独立结果；中断、忙碌、取消、失败保留原壁纸。
- [ ] Wi-Fi/BLE/授权状态来自现有服务；长任务期间仍可操作界面。
- [ ] 资源增量、内部堆和 PSRAM 峰值有实际记录；构建与既有交互/协议检查通过。
- [ ] 原生模拟器截图、板端 USB 预览、真实 LCD/触摸测试分别报告；不能互相代替。

只有取得新的实板日志或用户测量反馈时，才更新主 [首板测试记录.md](首板测试记录.md) 与相关交接摘要。本文审核完成、代码编译成功或模拟器截图均不等于实板通过。

## 11. 来源与后续维护

外部参考读取日期为 2026-09-20，链接跟随仓库 main；本稿的具体参数是 CABadge 自身决策，不随上游品牌文件自动变化。

- [VoltAgent 仓库与集合说明](https://github.com/VoltAgent/awesome-design-md)
- [Apple DESIGN.md](https://github.com/VoltAgent/awesome-design-md/blob/main/design-md/apple/DESIGN.md)
- [Vercel DESIGN.md](https://github.com/VoltAgent/awesome-design-md/blob/main/design-md/vercel/DESIGN.md)
- [Linear DESIGN.md](https://github.com/VoltAgent/awesome-design-md/blob/main/design-md/linear.app/DESIGN.md)

项目依据：

- [用户端设计准则](用户端设计准则.md)、[无线与本地管理任务](CABadge_BLE_WiFi_开发提示词.md)
- [当前 v5 设计实现说明](firmware/UI_DESIGN_V5.md)、[固件交接](firmware/HANDOFF.md)
- [共用 UI 与色值](firmware/ui/badge_ui.h)、[圆屏页面](firmware/ui/badge_ui.c)、[无线页面](firmware/ui/wifi_panel.c)
- [手机管理页面](firmware/usb_screen/device/src/wallpaper.html)、[板端显示路径](firmware/usb_screen/device/src/main.c)
- [分区表](firmware/usb_screen/device/partitions.csv)、[v5 构建记录](firmware/build/v5-device-build.log)

后续修改本文时，记录日期与设计理由；新增参数标明目标或实测，不用设计文档覆盖历史测试事实。先审核本稿，再安排 UI 实现。
