# CABadge 产品与界面规范

适用版本：项目 v1.1.0 / 内部固件 7.9.2-memory。本文整理现有产品方向和后续修改必须保持的约束，不表示所有交互已完成验收，也不授权新增功能或重设计。

## 产品重点

电子吧唧以壁纸、角色图和协会名片展示为核心。静置时减少常驻操作元素，需要操作时再展开面板；结束操作后可以回到完整展示状态。优先级是：图片展示、操作可靠、状态真实、触控响应、视觉一致、装饰动效。

应用入口已包含 Grok、MC 二维地图与奶蛙矿工；它们是独立功能，不改变壁纸展示作为主界面的定位。蓝色协会名片继续保留。

## 页面与操作

|区域|用途与约束|
|---|---|
|展示页|显示当前壁纸或蓝色名片，保持清晰完整，不常驻调试信息|
|控制中心|下拉进入快捷操作；状态必须来自真实服务|
|功能面板|上滑进入壁纸、设置、应用等操作入口|
|壁纸管理|浏览、选择、应用、删除职责清楚；当前支持多图，浏览支持双向循环|
|设置与连接|一致的标题、返回方式、控件反馈；保留 Wi-Fi、BLE、手机管理及显示设置|
|手机管理|同网或设备热点访问本地网页；当前为开放热点、无授权码/倒计时|
|动态应用|本体实时绘制，退出后恢复普通界面和状态更新|

首页展示切换与壁纸库选图是不同任务，不应把首页每次左右切换都当成更换已应用壁纸。具体现有行为见 [固件入口](firmware-v7/README.md) 和 [应用说明](firmware-v7/APPS.md)。

## 视觉约束

圆屏为 360×360。重要文字和点击区域需位于圆内安全区；不可仅按矩形画布判断边缘是否可用。长 SSID、错误信息与较多列表项仍需可读、可滚动，不能挤出边缘。

当前语义色定义在 [badge_ui.h](firmware-v7/ui/badge_ui.h)：背景 #F4F6F8、内容面 #FFFFFF、主文字 #1B2430、次文字 #5B6778、强调色 #365B86、选中底色 #DEE8F5、分隔线 #DCE3EC。新增同类控件应复用既有字号、圆角和布局，不从外部设计示例另起一套。

用户壁纸保留原色；系统反馈用统一语义表达选中、进行中、成功和失败。设备连接、上传和应用必须以真实结果为准，不能因按钮动画播放完就显示成功。

## 动效与触摸

- 保留现有展开、收起和页面位移动效；流畅度与跟手优先于缩放、透明度和复杂叠加。旧 Carousel 不是当前必须恢复的设计要求。
- 静止时使用真实 LVGL 控件。缓存位图只服务转场，不形成永久截图界面。
- 缓存准备好且路径适用时使用 Direct Compositor；缺失或不适用时安全退回真实控件路径，不在用户触发时同步生成两张截图。
- 减少动态开启时直接到终态，不改变业务结果。
- 拖动后松手不得误触按钮；滑条/列表操作不得同时触发页面导航。转场期间真实控件不应提前响应。
- 中断、退出和取消必须恢复正确目标页、输入归属及显示所有权；动态状态在转场结束后同步。

上述为行为约束，不能据此推断所有边界手势已测试通过。动画参数的修改应单独获得任务范围，不随文档维护调整。

## 资源与真实设备边界

外部 16 MB 是 Flash，8 MB PSRAM 是运行内存，不能混称为剩余 RAM。设备信息和开发工具应说明容量口径。保留多图壁纸库；不恢复旧单图覆盖模型。

USB 用于烧录、状态/控制与壁纸传输；旧实时像素镜像、壁纸读回不作为当前产品入口。手机管理保持现有原生 Web 页面，电脑工具保持原生 Windows 形态。

实际背光由 PWM 控制，旧 5% 测试上限已经取消。没有 TE，不能用提交 FPS 或 DMA 完成证明绝对无撕裂。GUI 刷新率、完整传输次数与屏幕扫描频率应区分。

## 验收与历史

修改界面后检查真实设备的显示、点击、慢拖/快滑、连续操作、返回与网络状态更新；只做任务所需的基本检查，不默认启动大规模 benchmark。构建或协议测试不能替代手感和目视结果。

当前限制见 [KNOWN_ISSUES](docs/KNOWN_ISSUES.md)，实际结果见[首板记录](首板测试记录.md)。早期 Apple/Vercel/Linear 分析与 Bencho 选型属于参考研究，不是官方嵌入式标准，也不覆盖当前取舍。

[重写前设计稿（历史固定快照）](https://github.com/nbtca/CABadge/blob/107f518cade6b85d44fc918d985363973a5d8b22/DESIGN.md) · [当前架构](UI_ARCHITECTURE_V7.md)
