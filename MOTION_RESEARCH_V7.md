> 历史设计/阶段记录：保留当时版本、方案和测量值，不作为当前安装或验收说明。当前正式版为 v1.1.0 / 7.9.2-memory，参见[文档索引](docs/INDEX.md)。

# CABadge v7 动效选型与架构修订

日期：2026-09-20。下文保留最初研究参数。R19 已在独立 v7.1.0-preview 落地既定清单；实现与采样说明见 [当前交接](firmware-v7/HANDOFF.md)。软件验证不代表 MCU 或实体屏性能已验证。

后续决策：用户已确定采用 PC LVGL + USB 实板服务联调，并要求按实机预算限制模拟器帧率，详见 [最新架构](UI_ARCHITECTURE_V7.md)。下文保留旧像素镜像带宽分析作为选型依据；它不再是 v7 主动效预览路径。

结论：采用 Bencho Carousel 的二维环绕卡片效果作为壁纸选择器；保留全屏壁纸首页、蓝色名片、下拉控制中心和上滑功能面。改造重点是跟手、连续转场和中断接续，而非给旧页面补淡入。

## 1. 实际研究范围

使用 Edge 浏览器打开 [Bencho](https://bencho.dev/) 首页的 30 个公开组件详情，触发各自的演示播放，保存状态截图；另打开 Finds 和 Bench。Finds 本次未加载出条目，Bench 查看了画布与引导，未登录或编辑保存。以下不是对登录后内容或所有历史作品的全覆盖声明。

- 30 项截图总览（本地历史资料，未入库：`outputs/ui-v7-bencho-research/catalog-overview.png`）
- 实际网站动效录像，约 33.6 秒（本地历史资料，未入库：`outputs/ui-v7-bencho-research/bencho-reference-motion.mp4`）：Carousel 0:00、Create menu 0:03.7、Liquid toggle 0:07.4、Slosh slider 0:11.1、Icon bar 0:15.9、Inline confirm 0:19.6、Notify 0:23.2、Pull to refresh 0:26.9。
- 浏览记录（本地历史资料，未入库：`outputs/ui-v7-bencho-research/browser-tour.json`）、录像章节（本地历史资料，未入库：`outputs/ui-v7-bencho-research/video-chapters.json`）。录像来自网站演示，不是 CABadge 原型；页面之间的导航等待已移除，不能作为性能基准。
- 结合浏览器实际画面、公开前端实现、项目 LVGL 9.4.0 源码和 USB 显示路径评估。没有将网站组件代码引入固件。

## 2. 选用的七组动效

表内时间与尺寸是 CABadge 初始设计值，需预览调校；不是宣称与参考网站逐毫秒一致。手势拖动不套缓动曲线，位置直接跟随输入。点击触发的位移初始采用 cubic-bezier(0.32, 0.72, 0, 1)。

| 位置 / 参考 | 当前问题与目的 | 频率 | v7 动作与初始参数 | LVGL / Web 实现与减少动态 |
|---|---|---|---|---|
| 壁纸选择：[Carousel](https://bencho.dev/?c=carousel) | 普通切图缺乏空间关系；让前后壁纸与当前选择可见 | 进入选壁纸时 | 中央大图、侧后方小图；沿二维椭圆轨迹轮转；松手吸附约 180–280 ms；静止时停止刷新 | 图像位移、缩放、层级排序；同一进度驱动。减少动态时保留明确选择与前后按钮，取消环绕运动 |
| 上下抽屉：[Create menu](https://bencho.dev/?c=liq-create) 与 [Pull to refresh](https://bencho.dev/?c=pull) | 突然出现整页；需要看到面板来源并允许撤回 | 高频 | 面板跟手展开；点击展开约 240 ms；释放后 180–240 ms 收敛；开合距离 35% 或明确同向甩动决定目标；仅边界有轻阻尼 | 取形状连续和阈值反馈，不新增下拉刷新功能。主要移动面板；圆角 24→16 px 可选；减少动态时直接展开，无分项错峰 |
| 无线开关：[Liquid toggle](https://bencho.dev/?c=liq-toggle) | 开关状态生硬；需要触发与回执区分 | 高频 | 滑块移动约 160 ms；中段宽度最多增加 10%，约 100 ms 恢复；执行中显示短状态 | LVGL 小对象 x/width 与背景色；不使用 SVG 胶融滤镜。减少动态时只变状态色；失败显示真实状态 |
| 预览亮度：[Slosh slider](https://bencho.dev/?c=slosh) | 拖动反馈机械；加强手指与滑块关联 | 高频 | 手柄与数值即时跟手，只有装饰填充允许最多 4 px、80 ms 的短尾随；释放后数值立即固定 | 复用 slider，不用波浪纹理；绝不让惯性继续改变亮度。减少动态关闭尾随 |
| 手机管理页的既有分区切换：[Icon bar](https://bencho.dev/?c=icon-bar) | 选中态突然跳位；明确当前分区 | 中频 | 单个选中底板移动 180 ms，前缘先走、后缘随后；只有确需分区控件时采用 | 手机用 CSS transform；圆屏首页不增加常驻底栏。减少动态只改选中态 |
| 中断上传等必要确认：[Inline confirm](https://bencho.dev/?c=confirm) | 弹窗割裂上下文；原地说明影响 | 低频 | 原操作区 200 ms 展开确认，取消 160 ms 回收；操作区不足以保持 44 px 热区时改独立确认页 | 对象尺寸与内容透明度；不新增恢复出厂等业务功能。减少动态直接展开 |
| 应用壁纸 / 保存反馈：[Notify](https://bencho.dev/?c=toasts) | 操作结束缺乏明确回执；避免到处弹提示 | 中频 | 原按钮在提交、处理中、已应用间过渡 160–180 ms；成功短暂保留约 1.2 秒 | 文案与局部透明度，可固定宽度避免布局跳动；仅服务成功才显示成功。不采用摇铃；减少动态直接换文案 |

最高优先级是 **Carousel + 抽屉跟手 + 展示页连续切换**。其余小动效不能挤占这三项的流畅度预算。

## 3. Carousel 在圆屏上的具体设计

### 用户看到什么

进入壁纸选择页后，当前候选壁纸在中央，两侧露出其他壁纸。水平拖动时，中央卡片沿弧线退到后方，下一张逐渐放大并来到中央；前后遮挡随位置改变。松手后吸附，不突然换图片。点两侧可见卡片也能选择；可见部分太小时，使用独立的至少 44×44 px 前后按钮，不把细小露边当唯一触摸入口。

选择页底部显示“应用”或“已应用”；浏览候选不立即修改持久化壁纸。应用成功后回全屏展示。首页没有持续轮播、自动换图或两侧缩略图。名片仍与全屏壁纸横向切换，不能混入壁纸资源列表。

初始构图：360×360 坐标，中间卡片中心约 (180, 160)，最大 180×180 px；轨道水平半径约 80 px，后退高度最多 22 px，最小比例 0.64。底部应用按钮约 112×44 px、中心 (180, 294)。这些是原型起点，完整热区仍须执行 DESIGN 的安全圆检查。

参考网站主要靠二维位置、缩放和遮挡形成纵深，附加漂浮与鼠标倾斜并不是核心轮转所必需。v7 保留核心轮转，不做持续漂浮或真实透视。缩略图按裁切策略生成，不能拉伸原壁纸。

### 最少需要的状态

- 一个连续的轮转位置 `turn`，一个释放目标 `target`，当前拖动速度与输入归属。
- 当前候选 `selected` 与服务确认的 `applied` 分开；浏览和动画不写 Flash。
- 每张有效壁纸一个图像对象；当前能力仍是两张内置加一张上传壁纸，不把壁纸双槽误当用户相册。
- 一张时不轮转；两张时保证遮挡排序稳定；环绕跨首尾时保持连续位置，不用取模后的位置直接跳动画。

可用下列二维关系计算卡片，所有卡片由同一个 `turn` 更新：

```text
angle = (index - turn) × 2π / count
front = (cos(angle) + 1) / 2
x = center_x + sin(angle) × orbit_radius
y = center_y - (1 - front) × 22
scale = 0.64 + 0.36 × front
z = front 越大越靠前；相等时采用稳定次序
```

此公式表达核心轨迹，不是完成的移植代码。左右方向、两张壁纸情况下的露边、裁切与触摸需要在 LVGL 原型中验证。最多三张 180×180 RGB565 缩略图的纯像素约 190 KiB，另计描述符、解码与变换临时内存；不能把这个数字当全部运行时占用。

## 4. 为动效调整的最小架构

```text
现有输入 → 手势归属（展示切换 / 抽屉 / Carousel / 控件）
                     ↓
             拖动进度或释放目标
                     ↓
     LVGL 原生动画 / 必要的小型单变量收敛函数
                     ↓
       同一次更新：位置、缩放、层级、局部透明度
                     ↓
          共用 LVGL 场景 → SDL / USB / LCD

现有设备服务 → 真实状态快照 → 页面内容和操作结果
```

只需修复导航生命周期和增加少量页面交互状态，不建设通用动画框架、场景引擎或新的业务状态机。

1. **保留转场两端。** 本轮查看的共用工作区 `badge_ui_page()` 先隐藏全部页面，再让新页从 y=12 移至 0；这解释了当前“硬切再补一点动画”的结构问题。它是变化中的 v6 工作区证据，不冒称冻结的 v5 原码。v7 在出入页完成移动后才隐藏旧页。
2. **一个手势一个主人。** 初始移动约 10 px 后锁定明确方向；Carousel 的水平拖动归自身，滑条归滑条，列表使用原生滚动。两个抽屉互斥。详情页不触发全局抽屉。
3. **同组属性共享进度。** 不分别开启多条互不知情的位置、缩放、遮挡动画。按当前可见位置反向接续，不重置至起点，不锁住输入等动画播完。
4. **先用原生能力。** 列表用 LVGL 滚动、惯性、吸附；点击转场用 `lv_anim`。原生缓动能保证位置连续，但不自动继承释放速度。只有 Carousel/抽屉原型证明需要时，补一个有界、按实际时间步长计算的单变量阻尼收敛函数；不能把 ease-out 称为物理弹簧，也不能让它与原生惯性同时写同一位置。
5. **生命周期收尾明确。** 页面退出取消所属动画；完成回调核对对象仍有效；释放或替换壁纸资源前先解除图像引用。后台连接结果更新状态，不强行打断用户当前页面。
6. **内容与操作状态分离。** 选择不等于应用，开启蓝牙不等于已连接，上传 100% 不等于保存完成。动效展示已有服务阶段，不捏造阶段和进度。

LVGL 9.4 提供动画回调、缓动、滚动吸附与对象变换；本项目软件绘制路径可以承载上述二维方案。整组对象缩放/透明度可能触发临时绘制层，优先小图像与局部控件，不默认把整个屏幕截图后变换。[LVGL 动画](https://docs.lvgl.io/9.4/details/main-modules/animation.html)、[滚动](https://docs.lvgl.io/9.4/details/common-widget-features/scrolling.html)。

## 5. USB 预览与硬件限制

**可实现动效不等于当前 USB 能完整呈现高帧率大画面。** 当前 `firmware/usb_screen/device/src/main.c` 在 LVGL flush 内逐条带同步发送像素，发送完成才调用 flush_ready；传输等待会影响 UI 服务输入的及时性。接收端约 30 ms 轮询，也会影响端到端反馈。

一帧 360×360 RGB565 为 259,200 字节。USB Serial/JTAG 为 Full-speed 12 Mbit/s：即使不计协议开销，连续未压缩整帧的理论上限也只有约 5.8 帧/秒；30 帧/秒需要约 7.78 MB/s。此处是理论带宽核算，不是实测帧率；局部刷新通常传得更少。[Espressif 技术参考手册](https://documentation.espressif.com/esp32-s3_technical_reference_manual_en.pdf)。

- SDL2：验证同一套 LVGL 的构图、跟手、取消、方向与动画节奏。
- 用户选定的 PC + 实板模式：SDL2 本地运行共用 UI，USB 只传动作、真实状态和必要资源；补齐服务桥接后用于主要联调。PC 渲染帧率与板端服务延迟分别记录。
- 板端 USB：验证真实服务、内存、输入延迟与像素一致性；单独报告带宽导致的掉帧。
- 实体 LCD：接屏后测真实刷新和触摸；流畅目标先定 30 fps，尚未承诺达到。
- 旧像素镜像的异步发送优化暂不作为主路径的前置任务。如以后继续优化镜像，不能任意丢弃局部更新，否则会残留旧像素。新模式是在 PC 运行同源 LVGL，明确标识本机渲染，不是给板端帧补间或另写一套 UI。

保持现有 Flash 分区、壁纸格式和服务接口。8 MB PSRAM 不等于全可供 UI；全帧 RGB565 约 253 KiB，ARGB8888 约 506 KiB，加上变换临时层很容易累加。中文字库走已有生成流程，不为动效引入新大字库。静止后停止装饰计时器，息屏停止绘制。

### 5.1 受限模拟器的刷新预算（2026-09-20 补充）

**初始采用 30 FPS 上限 + 实际脏矩形传输预算，再用板端绘制计时校准。** 不能仅靠 ESP32 型号准确预测任意动效帧率，更不能将 PC 的帧率宣称为实机性能。接口刷新能力与绘制能力是两项限制，Espressif 的 LCD FAQ 也明确区分二者。[官方 LCD FAQ](https://docs.espressif.com/projects/esp-faq/en/latest/software-framework/peripherals/lcd.html)。

本轮重新核对的证据：

| 项目 | 依据 | 含义 |
|---|---|---|
| 模组 | 根目录 LCD 调试资料中 `1、规格书 W180TE010I-18Z(CTP-A1) SPEC规格书.pdf`，第 4、11、12 页 | ST77916，360×360，四线 QSPI；有 TE 引脚不等于本板已经接入 TE 同步 |
| QSPI 时序 | 同目录 `2、显示屏驱动芯片数据手册 ST77916_SPEC_V1.0_20220830.pdf`，第 39 页；已渲染核对表格 | 写时钟周期最小 16 ns，单从该条件得出的上限为 62.5 MHz；还须满足数据建立/保持等约束 |
| 供应商示例 | Arduino 包的 QSPI 默认 80 MHz；主程序 `gfx->begin()` 使用默认值 | 示例值与上述手册条件不一致，不能据此把 80 MHz 当本板保证值；预算先用 40 MHz，未改现用时钟 |
| 当前点屏诊断 | `firmware/bringup/src/display_diagnostic.c` | 10 MHz、四线传像素，属于保守诊断；此前仍未获得可见显示通过证据 |
| 当前用户固件默认配置 | `firmware/usb_screen/device/sdkconfig.defaults` | CPU 240 MHz、Octal PSRAM 40 MHz、16 MB Flash；不是旧诊断的 160 MHz |
| 当前模拟器 | `firmware/simulator.c`、`firmware/lv_conf.h` | 1040×760 ARGB8888 桌面画布，软件 SDL；不能直接等同 360×360 RGB565 板端耗时 |
| LVGL 默认周期 | `vendor/lvgl-9.4.0/src/lv_conf_internal.h` | 33 ms 刷新周期，属于调度间隔，不是实测帧率或屏幕扫描频率 |

**屏幕自身扫描频率：** 供应商初始化及 `panel_init.h` 中正常模式 C0=0x80、C1=0x10、C2=0x37，即 porch=16、RTNA=55。驱动手册第 238 页给出公式；若本模组 Y=360 且 tcon 周期沿用手册示例 400 ns，则推算约 `1e9 / ((2×360 + 2×16)×55×400) = 60.44 Hz`。这是带时钟假设的名义估算，手册注明频率偏差 ±5%；供应商特殊寄存器设置尚未完整确认，不能当实测值。尤其不能直接引用同页 Y=390、porch=32 的查表频率。接屏后以实际扫描/TE 等可用测量或供应商确认为准；不要为本次模拟器需求修改初始化。

**链路纯像素预算：** 每像素 16 bit，四条数据线，不含指令、片选间隔、DMA/拷贝、绘制与等待扫描的时间。

| QSPI 时钟 | 纯数据带宽 | 360×360 整帧传输 | 仅传输的整帧理论上限 |
|---|---|---|---|
| 10 MHz，当前诊断配置 | 5 MB/s | 51.84 ms | 19.29 FPS |
| 20 MHz，比较用配置 | 10 MB/s | 25.92 ms | 38.58 FPS |
| 40 MHz，拟用预算，待验证 | 20 MB/s | 12.96 ms | 77.16 FPS |

30 FPS 每帧约 33.33 ms，在 40 MHz 下整帧纯传输占约 12.96 ms，剩约 20.37 ms 给其他工作；不代表复杂 Carousel 一定能在剩余时间内绘制完。10 MHz 情况下，全屏更新即使绘制零耗时也达不到 30 FPS。小区域更新按实际面积计算；圆形可见区域不自动降低矩形传输字节数。

实施保持简单：

1. 单独限定圆屏为 360×360 RGB565，软件绘制，初始按 16 行局部缓冲比较；窗口放大只显示同一像素结果。保持已有字体、图片及缩放算法，不用 PC 专属效果替代。
2. 一个单调时钟调度器控制刷新上限与待完成传输预算；提交字节数从真实 flush 区域累计。初始按绘制/传输串行的保守预算估算；之后只有实际双缓冲和 DMA 重叠得到验证，才按流水线测量修正，不能凭空抵消传输时间。
3. 有了板端绘制计时后，更新间隔按 `max(33.33 ms, 对应场景绘制耗时 + 传输耗时 + 驱动开销)` 约束。无板端计时时，绘制耗时明确未知，PC 实际绘制耗时仅用于保持程序正常运行，不能填成 MCU 耗时；不造一个“每像素 CPU 系数”假装精确。
4. 超出预算时减少实际更新次数、动画按真实时间继续，支持反向接续；不得积压过时帧、事后加速播放或靠插帧改善观感。使用非阻塞到期检查，不让显示等待阻塞 USB 状态与输入处理。最终像素转换到桌面输出格式只负责显示。
5. 在开发工具中保留帧率/链路预算的校准参数，不把性能选项放进吧唧用户页面。默认 30 FPS；另用 20/15 FPS 做卡顿容忍检查，这两个值只是压力预览档，不称为实机测量。10 MHz 诊断链路按字节预算自然限速。
6. 接屏前可以复用主控做内存渲染基准：同一 v7 场景和操作序列、相同 RGB565/缓冲与 PSRAM 配置，关闭像素镜像，记录软件绘制时间、内存峰值；同时覆盖 Wi-Fi/BLE 工作状态。只回传汇总计时，不传图。接屏后另测 flush/驱动/触摸与扫描节奏，再校准预览。

验收分别给出呈现 FPS、帧间隔 P50/P95、绘制/flush 耗时、实际提交字节数、输入到画面更新延迟与内存。高帧率不是唯一指标：一段约 240 ms 转场在受限预览中仍须保持方向连续、可取消、无状态跳变。当前只完成资料核算与设计约束，尚未实现限帧或运行这些基准，不更新实板通过记录。

## 6. 全部公开组件筛选记录

“借鉴”只表示相关原则进入候选，不代表新增业务功能或已移植。

| 组件 | 结论 | 理由 |
|---|---|---|
| Magnetic select | 不选 | 磁吸鼠标反馈不适合圆屏触摸目标 |
| Carousel | 选用 | 用户指定；二维轮转适合壁纸选择 |
| Escape button | 不选 | 逃跑按钮阻碍可靠操作 |
| Canvas toolbar | 不选 | 当前产品无画布编辑任务 |
| Drag stepper | 不选 | 已有滑条足够，不引入隐藏拖动语义 |
| Notify | 借鉴 | 原地表达真实操作阶段，去除摇铃装饰 |
| Reorder list | 不选 | 目前没有用户排序业务 |
| Wheel | 不选 | 当前无对应选择任务，不为动效增加功能 |
| Selection list | 暂不选 | 网络列表先保证稳定阅读，不做逐行弹入 |
| Liquid toggle | 借鉴 | 小尺度滑块形变；无需胶融滤镜 |
| Checklist | 不选 | 无清单业务 |
| Assignees | 不选 | 无多人分配业务 |
| Slide to confirm | 不选 | 普通连接/应用不值得增加滑动确认成本 |
| Search | 暂不选 | 当前无须新增搜索交互 |
| Palette | 不选 | 当前无调色业务 |
| Slosh slider | 借鉴 | 跟手与短尾随，去除水面动画 |
| Radial menu | 不选 | 圆形外观不等于小屏适合密集放射目标 |
| Inline confirm | 借鉴 | 有实际中断后果时原地确认 |
| Magnifying dock | 不选 | 鼠标悬停放大与隐藏系统 UI 冲突 |
| Progress ticks | 暂不选 | 使用既有真实上传进度，不添加环形装饰 |
| Command bar | 不选 | 小屏无键盘命令面板需求 |
| Dragging ball | 不选 | 无任务收益 |
| Todo tower | 不选 | 无待办业务；叠层消耗不值得 |
| Create menu | 借鉴 | 形状与内容连续展开，用于抽屉 |
| Aspect ratio | 不选 | 圆屏比例固定；手机裁切沿用已有任务 |
| Tilt card | 不选 | 鼠标倾斜不适合触摸；蓝色名片保留自身图形 |
| Now playing | 不选 | 无音乐播放业务 |
| Pull to refresh | 借鉴 | 取拖动阈值/撤回反馈，不新增刷新动作 |
| Icon bar | 借鉴 | 手机必要分区的滑动选中底板 |
| Range dial | 不选 | 无双端范围设置任务 |

不采用的五类效果：鼠标悬停专属；持续漂浮与无目的循环；高成本模糊/胶融/真三维；缩小触摸目标的密集装饰；要求新业务才能成立的演示组件。LVGL 的 3D texture 依赖 OpenGL ES 后端，当前 ESP32 软件绘制配置并不具备该路径；这不影响二维 Carousel。[LVGL 3D texture](https://docs.lvgl.io/9.4/details/widgets/3dtexture.html)。

## 7. 审核后最先验证什么

1. 独立 v7 工作区，仅先实现 Carousel、展示页横切和上下抽屉，沿用真实蓝色名片。录制共用 LVGL 的实际交互，不能拿网站录像当交付效果。
2. 测试拖一半反向、释放后立即反拖、快速连点、两抽屉互斥、点小卡片、跨首尾、单图/双图/三图，以及减少动态。验收无空白帧、瞬移、误应用或残留半开状态。
3. 记录渲染耗时、输入到更新延迟、最低剩余内存、最大连续可分配块和重复开关后的资源变化。几何检查完整热区，中文与长名称不裁切。
4. 分别记录 SDL、USB 和未来实屏的效果与限制。核心交互达到要求后再扩展开关、滑条和服务反馈，不以简化成硬切掩盖性能问题。

本轮只新增研究材料并更新架构/规范。没有改 UI 源码、固件、硬件文件或实板测试结论；下一阶段仍等待架构审核。
